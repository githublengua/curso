function isValidnumber(p) {
    typeof p == "boolean" ||
    typeof p == "object" ||
    )}

    
}

/ ** Función sumar
* @descripción recibe dos parámetros y devuelve la suma de ambos
* @param  {número}
* @param  {número}
* @returns  {number}
 * 
 * /
función  sumar ( a  =  0 , b  =  0 ) {
    if (typeof a == "bolean" ||
    typeof b=="boolean"
    typeof a=="boolean"
    typeof b== "undefined"
    typeof a== "undefined"
    a== NaN ||
    b ==NaN ||
    isNaN(a)
    isNaN(b)
    )
    / * vamos a c = a + b
    devuelve c * /
    devuelve +a + +b
}

/ ** Función restar
* @descripción recibe dos parámetros y devuelve el resto de ambos
* @param  {number}  a 
* @param  {number}  b 
* @returns  {number}
* /
función  restar ( a  =  0 , b  =  0 ) {
   devuelve a - b
}

/ ** Funciona multiplicar
* @param  {number}  a 
* @param  {number}  b 
* @returns  {number}
* /
función  multiplicar ( a  =  0 , b  =  1 ) {
   devuelve a * b
}

/ ** Funcion dividir
* @param  {number}  a 
* @param  {number}  b 
* @returns  {number}
* /
función  dividir ( a  =  0 , b  =  1 ) {
    devolver a / b
}

/ ** Función Mostrar
* @descripción Recibe un texto y lo saca por pantalla
* @param  {cadena}
* @returns  {void}
 * 
 * /
función  mostrar ( cadena  =  ' ' ) {
    consola . log (cadena)
}

módulo . exportaciones  = {}
módulo . las exportaciones . sumar  = sumar
módulo . las exportaciones . restar  = restar
módulo . las exportaciones . multiplicar  = multiplicar
módulo . las exportaciones . dividir  = dividir
© 2019 GitHub , Inc.