function calcularSC(x, y) {
    return (x*x) + (y*y)

for (let i = 0; i < z.length; i++) {
    let  array[i];
    x=
}
    
}
}

    

x = array
y = array 
console.log(calcularSC(4 ))
/**
 * 
 * @param  {array} resto 
 * @returns{number}
 */
function calcularSC(...resto) {
    let r = 0
    let aCuadrados
    calcularSC(1, 5, 7)
    console.log(resto)
    for (let i = 0; i < resto.length; i++) {
        const constante = resto[i];
       r += constant
        
    }
return {sumatotal:r,}

console.log(calcularSC(1,3,4,5))
    
}
// let aResultados = calcularSC
let {aCuadrados, suma}= calcularSC