api = interfaz de aplicaciones , es un lenguaje de programacion compuesto de bloques , por lo tanto un api de red (seria el conjunto  de instrucciones que se utilizan en red),

api rest = la importancia de las api se basa en que puedes hacer aplicaciones pero un 'back' demasiado extenso, por ello buscas un api concreto para que la aplicacion muestre los datos
api rest => es estandar por ello no necesitas buscar un api concreto `es el mas utilizado`

framwork es un entorno de trabajo , es un conjunto de instrucciones referentes al estilo de la pagina para organizarse por equipos , para esto se crean libreria de funciones(que son ampliaciones del lenguaje pero que no son estandar.)




//node\. (el nombre del archivo)sirve para testear en la consola sin pasar por internet.


//java script
//si quiero recorrer un array utilizo un for.
//si quiero recorrer un objeto utilizo un array.
//try  tras el try se pone la variable que queremos probar dentro del cath lanzamos un error se puede lanzar con console.log(errorp)
//despues de catch se pone el nombre de la variable a la que nos referimos llamada como querramos 
try{
 meses()
}
catch (e)
console.error(lo sentimos)
console.error(e.mensage)
)
console.log(gracias por venir
)
}
estudiar require se utiliza igualado a la funcion para referirnos a otro rchivo
console.clear - limpia la consola desde ese punto

// es una funcion para generar numeros enteros, recorta el primer valor y lo devuelve.

parseInt(3.4)
//parsefloat redondea el vaor que se le asigna


parseFloat(3.4)
//este objeto es un array un tipo de objeto que no se instancia es un objeto unico con un monton de valores 

//nos envia la parte integra de la expresion numerica sin el resto


Math.trunc(3.4)
//redondea igual que el parse int pero no obia las comillas si no que nos acerca al resultado

Math.round(3.1)
//redonde hacia arriva


Math.ceil(3.7)

console.log(Math.trunc(Math.random()*10));
