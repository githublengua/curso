/* function factorial() {
    factorial= 6
}
for (let i = 0; i < factorial.length; i++) {
    const x = array[i] * [i--];
  if (x=Factorial) {
      
  }
    
}
console.log(Factorial) */




let limite = 10
let acumulador = 0
let maximo = 100
let contador = 0
for (let i = 0; i <= limite; i++) {
    contador = i
    
    if ( acumulador + (i*i) > maximo) {
        break
    }
    acumulador += (i*i)
}


function ciclos() {
 
    var arrayU = new Array(3);
    var f;
    for (f=0;f<arrayU.length;f++) { // ARRAY LENGTH = 3 = FOR = 0, 1 , 3
      var v = prompt(acumulador);
      arrayU[f] = parseInt(v);
    }
    var total = 0;
    //var multiplicacion;
    //var number;
    for (f=0;f<arrayU.length;f++) {
    total = arrayU[f] + total;
    }
      console.log(total);    
  }
  




 const factorial = 120




function fact() {
    if (factorial = num) {
return function (){let limite = 10
let acumulador = 0
let maximo = 100
let contador = 0
for (let i = 0; i <= limite; i++) {
    contador = i
    
    if ( acumulador + (i*i) > maximo) {
        break
    }
    acumulador += (i*i)
}}

}    
    } else (return console.log('no es un numero') {}


    
 let acumulador = 0
let factorial = 120
let contador = 0
for (let i = 0; i <= factorial; i++) {
    contador = i
    console.log(i * (i))
    if ( acumulador * (i) >= factorial) {
        break
    }
    acumulador += (i)
}
 


 09 introduccion js