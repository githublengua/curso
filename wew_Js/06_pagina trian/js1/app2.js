export function app2() {
    console.log()

    //nodos del dom
    aBotones = document.querySelectorAll('.btn')
    aInputs = document.querySelectorAll('input')
    aOutputs = document.querySelectorAll('output')



//asignacion de los manejadores
aBotones.forEach(btn => btn.addEventListener(
    'clicl', onBtnClic))



    //manejadores y otros metodos
    const onBtnClic = function () {
        switch (ev.target.id) {
            case new circulo(aInputs[0].value)
                
                break;
    
            case new circulo
                break;
                case name ='circulo':
                    break
        }
    }
    console.log(name)
}